apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: shark-socket
  labels:
    app.kubernetes.io/name: prometheus
    app.kubernetes.io/part-of: shark-socket
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: prometheus
  ports:
    - name: web
      port: 9090
      targetPort: web
      protocol: TCP
