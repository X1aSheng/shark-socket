version: "2"
linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gocritic
  exclusions:
    paths:
      - examples/
formatters:
  enable:
    - gofmt
    - goimports
run:
  timeout: 5m
