# Mosquitto security configuration for shark-socket development
# Default: disable anonymous access; configure a password file for production.

allow_anonymous true
listener 1883
protocol mqtt

# For production, replace the above with:
# allow_anonymous false
# password_file /mosquitto/config/passwd
# listener 1883
# protocol mqtt
